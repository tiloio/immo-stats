{
  "name": "immo-stats",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=src/index.js --platform=node --format=cjs --target=node16 --external:puppeteer",
    "start": "npm run build && node src/index.js"
  },
  "keywords": [],
  "author": "Tilo",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^13.1.2"
  },
  "devDependencies": {
    "@types/puppeteer": "5.4.4",
    "esbuild": "0.14.14"
  }
}
